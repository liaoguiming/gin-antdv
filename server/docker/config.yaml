# docker下gin项目配置文件

# mysql connect configuration
mysql:
  username: root
  password: 'Liaoguiming1234!'
  path: mysql
  db-name: 'laravel'
  config: 'charset=utf8mb4&parseTime=True&loc=Local'
  max-idle-conns: 10
  max-open-conns: 10
  log-mode: false

# redis configuration
redis:
  addr: redis:6379
  password: ''
  db: 0